; The equivalent of 'car' and 'cdr' in clojure
; are the functions 'first' and 'rest'
; respectively

(def car first)

(def cdr rest)

; However we will keep the function 'first' and
; 'rest' for convenience
